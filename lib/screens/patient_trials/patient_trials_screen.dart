import 'package:flutter/material.dart';

class PatientTrialsScreen extends StatelessWidget {
  const PatientTrialsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("trials");
  }
}
